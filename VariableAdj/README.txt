1. Input is a varying (f,A)
2. It is already generated and stored by a separate code in the DataGenerator folder
	- There is a code that takes in the MNIST data and creates a pickled file
	- The next code takes a picked file as the input and generates + saves a graph with required number of nodes 		  and digits
	- The created file needs to be taken out of this folder and placed with the main.py codes
3. The ipython files were just used for testing smaller modules
4. There is really no validation set. The test set is referred to as te validation set in the codes
